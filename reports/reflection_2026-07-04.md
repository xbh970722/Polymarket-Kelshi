# 每日复盘 2026-07-04

## 今日数字

- 今日已实现: **$+4.44** | 余额 $17.62 | live 敞口 $1.81
- 今日成交 36 笔 | 今日结算 39 笔（settle 命令未额外发现迟到结算，0 settled / 5 still open）
- 分通道已实现:
  - shortcycle（lag_only 15m/hourly 量化）: $+0.00
  - favorites（方向中性收割极端价格）: $+1.60
  - weather（NWS 温度模型）: $+0.87
  - ensemble（多模型辩论）: $+1.97

注：余额从昨日 $3.32 跳到 $17.62 主要是本金随 39 笔结算解锁回流，不代表今日盈利本身；今日纯已实现盈亏是 $4.44。

## 逐笔复盘

**Alito 退休传闻 (KXALITOANNOUNCERETIRE)** — yes x81 + x1 @ 11c，模型 q=0.17 vs 市场 0.10，止盈 take_profit@15c 平仓，合计 pnl +$1.97。**这是技术判断**：四个估计器一致认为被撤回的 Totenberg 报道仍是高于市场定价的真实信号，规则核实了"无论生效日期，宣布即算数"，且用 swing 止盈提前落袋而非赌到最终结算——过程和结果都站得住。今天同一市场又新开了一笔 x1 @11c 的 "首次真钱下单验证" 仓位（rationale 明确写着这是买单路径测试，非信号驱动），目前 open，**不应计入策略胜率统计**。

**天气通道（4 笔结算，n=3 in Brier）**:
- KXHIGHPHIL no @36c → settled(no) +$0.62，模型 q=0.37 vs 市场 0.65，模型逆市场方向且赢了——技术判断，NWS 站点数据驱动的分歧被验证。
- KXHIGHNY yes @12c → settled(no) −$0.38，模型自身 q=0.37 本就不算高置信度，市场隐含 0.115，模型的"胜率"预期本来就不到四成，这笔亏损**不算错误**，是模型自己承认的不确定性兑现。
- KXHIGHNY no @35c → settled(no) +$0.63，模型 q=0.32（no 方向）vs 市场 0.655，方向对、赢。
- 样本仅 3-4 笔，天气 Brier 0.1271 明显优于市场 0.2861，但 n 太小，还不能说"跑赢市场"是稳定技能而非运气——需要更多样本再下结论。

**favorites 方向中性收割（32 笔结算，2 笔大额亏损）**:
- 30 笔按 83c-96c 收割极端定价方向，单笔 +$0.05~+$0.28，符合设计预期（不是预测方向，是吃市场对极端概率的错误定价残差）。
- 2 笔亏损集中触发尾部风险：KXETHD no@84c x2 → settled(**yes**) −$1.70；KXBTCD no@96c x2 → settled(**yes**) −$1.93。**这不是判断错误，是该策略结构性代价**：模型标的自己的置信度就是 84%/96%，即有 16%/4% 的既定概率会遇到这种尾部亏损。今日 32 笔里出 2 笔尾部，损失率大致落在校准区间内（mktcal 0.80-0.85 bucket 实际胜率 0.844 略高于隐含 0.823；见校准趋势）。**唯一值得警惕的是赔率不对称**：赢一次约 +$0.1~0.3，输一次可能 −$1.7~1.9，需要约 8-10 笔小赢才能覆盖 1 笔尾部亏损，今日 32:2 的比例仍是净正但边际不厚。

**污染样本（非策略信号，建议排除出统计口径）**:
- 18:31 "sell-path validation left net -2 YES (fill_count_fp parse bug)"，voided，$0 pnl，是卖单路径验证时因解析 bug 误判净仓，已作废。
- 18:39 Alito x1 "first-ever live order (buy-path validation); was never ledgered"，open，是买单路径验证单。
两笔都是并行进行的真钱下单链路验证（另有一批未提交的 src/live.py、src/ledger.py、src/kalshi_client.py 修改正在进行中——见"回归测试"一节），不是量化信号产生的交易，建议今后给纯路径验证走独立测试账本，避免和真实策略胜率混在一起统计。

## 校准趋势

**Brier(model) vs Brier(market)，favorites 按设计排除**：
- shortcycle: 0.0457 vs 市场 0.0141（n=16）—— 模型仍明显劣于市场，符合 SHORTCYCLE_DESIGN.md 里"1小时加密货币近乎随机游走，AI大概率无优势"的诚实先验，没有反转迹象。
- weather: 0.1271 vs 市场 0.2861（n=3）—— 模型优于市场，但样本太小，暂不能断定是稳定技能。
- ensemble: 尚无结算样本可算 Brier。

**favorite 分桶校准偏差（累计 574 个 favorite 样本，非今日单日）**:

| 区间 | n | 隐含概率 | 实际胜率 | 偏差 |
|---|---|---|---|---|
| 0.75-0.80 | 62 | 0.777 | 0.742 | −0.035 |
| 0.80-0.85 | 77 | 0.823 | 0.844 | +0.021 |
| 0.85-0.90 | 76 | 0.875 | 0.908 | +0.033 |
| 0.90-0.95 | 110 | 0.927 | 0.927 | −0.000 |
| 0.95-1.00 | 249 | 0.975 | 0.996 | +0.021 |

除 0.75-0.80 区间外，市场在高置信度收割区都轻微低估真实胜率（偏差为正），方向对做多 favorites 策略有利；但这是累计历史值，**没有留存历史快照，无法给出"趋势"（变好/变坏）**——这是个数据缺口，见明日提案。

**H9（午夜重开滞后假说）**：**今日无法验证**。shortcycle 通道全天 177 次扫描中出现 candidate 的有 135 次，但其中 114 次一进循环就撞上"budget: shortcycle daily budget reached"（$2 日预算在交易日开始后约 66 分钟、00:05-01:11 之间就被打满），真正跑到 lag_only 门检验的只剩约 21 次循环（集中在 00:05-01:11，恰好覆盖 H9 关心的午夜重开窗口），而这些检验里 **0 次通过**门槛（`|q_model−0.5| < |mid−0.5| + 0.02` 全部触发 continue，全天 0 笔 shortcycle 订单）。没有一次通过意味着没有样本可画"按小时的 lag 门通过率分布"——H9 目前既不能证实也不能证伪，只能说：现有 2pt 门槛下，午夜窗口里挂着的候选没有一个"模型比市场更确信"。建议见明日提案。

## 回归测试

- `tests/test_ledger_live.py` → **PASS**（ALL LEDGER LIVE-STATE TESTS PASSED）
- `tests/test_swing.py` → **PASS**（take-profit pnl=$+1.95, swing_pnl=$+1.95）
- 今日实际提交 547d435「6-Codex panel round 2: fix all 26 findings (money-path first)」**确实改动了订单相关路径**（src/ledger.py, src/pipeline.py, src/shortcycle.py, src/weather.py, src/blindai.py），建议人工重跑一次 `tests/test_failures.py` 确认失败路径覆盖仍然成立（本次复盘未跑，因为该测试涉及更广的故障注入场景，超出常规回归范围）。
- **额外发现**：工作区里还有一批**尚未提交**的改动（src/live.py 分页取仓位、client_order_id 支持；src/ledger.py 的 `has_open_position` 把 `unknown` 状态也纳入拦截、新增 `checkpoint()`；src/kalshi_client.py、src/pipeline.py 也有改动），看起来是另一轮"R3 五审"订单路径修复正在进行中，还没收尾提交。**本次复盘的 git 提交只包含新增的复盘报告本身，没有用 `git add -A` 打包这批未完成的代码改动**——避免在自动化复盘任务里意外提交别的会话正在进行、尚未测试完的订单路径代码。这批改动一旦提交，也需要人工重跑 `tests/test_failures.py`。

## 明日提案

1. **favorites 尾部风险的仓位不对称** — 现状：极端桶（84c/96c 附近）单笔亏损可达 −$1.7~1.9，是普通盈利笔的 6-10 倍。数据依据：今日 32 笔里 2 笔尾部亏损吃掉了约一半的当日净盈利。提议（不自动生效，留给用户拍板）：给 0.94+ 和 0.84- 的极端分位单独设置更低的单笔合约上限，用更多笔小额分散换取更平滑的盈亏曲线。
2. **shortcycle 日预算太小，H9 测不出数据** — 现状：$2/日预算在交易日开始后约 66 分钟耗尽，全天只有约 21 次循环真正跑到 lag_only 门，且 0 次通过。数据依据：177 次扫描里 114 次直接被预算拦截，候选数最多到 7 个但从未通过 2pt 门槛。提议：不改预算总额，而是把日志粒度加深到"记录每次门检验的 `|q_model−mid|` 具体差值（即便未通过）"，这样即使 0 笔成交也能看出候选离阈值有多远、是"差一点没过"还是"完全没边"——这是纯诊断性的日志改动，不影响交易行为。
3. **market-calibration 快照没有留存历史** — 现状：mktcal 只给出累计值，无法回答"favorite 分桶偏差本周是在变好还是变坏"。提议：复盘任务每日运行 mktcal 后把分桶偏差表存一份带日期的 snapshot（比如 `data/mktcal_snapshots/YYYY-MM-DD.json`），几天后就能画出偏差随时间的曲线，而不是只有一个累计快照。
4. **真钱路径验证单混进正式账本** — 现状：今日出现至少 2 笔明确标注"buy-path validation / sell-path validation"的测试单，掺进了 39 笔结算里的胜率统计口径（虽然本次复盘手动识别并排除了，但下次复盘的人/AI 未必会注意到）。提议：给这类纯技术验证单一个独立的 `title` 前缀（比如 `pathtest-`），复盘脚本按前缀自动排除，不用每次人工翻 rationale 字段找。
