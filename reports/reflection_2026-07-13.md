# 复盘 2026-07-13

## 今日数字

- 今日已实现 **$-0.92**（0 笔成交，5 笔结算/平仓）| 余额 $57.52 | live 敞口 $0.12
- 分通道：
  - shortcycle $+0.00（07-11 起停用，n 未变 35）
  - favorites $+0.00（停用）
  - weather **$+0.20**（3 笔结算，Brier 0.3103 vs 市场 0.3268，n=30）
  - ensemble **$-1.12**（2 笔 swing 平仓结算：MEDNOM 止损 -$1.40 + FEDDECISION 止盈 +$0.28）
  - h10 $+0.00（KILL 门，纯 shadow）
  - h15 $+0.00（**今日 21:55 触发 HARD STOP，累计已实现 -$4.56，暂停待复核**）
- reconcile：现金无漂移（$57.52 = 预期），1 个 ticker `BOOKED_TS NULL`（#62 ALITO，07-04 起已知未处理，纯记账字段缺失，非资金问题）。

**运维异常（本项目全部日志历史中唯一一次 >30min 空档）**：`quant_loop.log` 从 07-13 07:32:38 到 21:55:27 存在
**14.38 小时**无任何心跳记录 — 期间 MEDNOM 本应每小时触发一次 `REVIEW-DUE`（此前 00:35-06:35 共记录 7 次，逐小时不间断），
该窗口内彻底消失，说明循环确实停摆而非"无事可做"。git 提交也印证同一空档：`51a7045 quant loop 07-13 07:35`
到 `beeccdb swing manage 2026-07-14T03:56:11Z`（=本地 21:56）之间无任何 quant_loop/light-mark 提交。`watchdog.log`
最后一条记录是 `2026-07-12 20:27:29 watchdog loop START`，**07-13 当天 watchdog 本身无一条 RESTART 记录** ——
无法从仓库内证据判断是机器休眠/关机、watchdog 自身也挂了、还是被手动重启；21:55 复苏后 5 笔结算 + 2 笔 swing
退出（MEDNOM、FEDDECISION）在几分钟内集中触发，符合"断档后一次性补跑"而非静默空闲的特征。已列入明日提案首位。

## 逐笔复盘

**KXHIGHPHIL-26JUL12-B86.5** no x1@45c → 结算 no，pnl **+$0.53**。模型 q=0.30 vs 市场 q=0.56（分歧 26pt，
NWS 模型：mu 85.0F sigma 2.0，obs_max 82.4F vs 预报 85.0F）。**判定：技艺** —— 边际来自具体气象观测/预报输入
的真实分歧，不是噪声内的巧合，且分歧方向与结算结果一致。

**KXHIGHDEN-26JUL12-B96.5** no x1@63c → 结算 no，pnl **+$0.35**。模型 q=0.63 vs 市场 q=0.625（分歧仅 0.5pt，
W2 fade 策略）。**判定：运气/无信息含量** —— 模型基本等于市场价，这笔赢面等同于"按公允价小赚"，不构成模型
校准证据。

**KXHIGHNY-26JUL12-B84.5** no x1@66c → 结算 yes，pnl **-$0.68**。模型 q=0.66 = 市场 q=0.66（edge_net=0.0，
W2 fade 策略，rationale 里 tau=22h 的 99.5% band 触发进场，但入场时模型价与市场价完全相同）。**判定：运气
性亏损，非模型误判** —— 既然进场时无任何方向性分歧，这笔亏损不能记在"模型校准"账上，只能算 W2 fade 策略
本身在无边际时仍进场的机制问题（该策略仍在 τ-exit 预注册规格待验证期，07-23 前不擅自改）。

**KXMEDNOMJUL-26AUG01-TJAC** no x5@49c → **止损平仓@24c**（非终局结算），pnl **-$1.40**。模型 q=0.19 vs
市场 q=0.52（分歧 33pt）。**判定：既非纯运气也非纯"模型错"——这正是 07-11 五人会（1 Fable+2 Opus+2 codex）
判定 CORE/ensemble 结构性负 EV 的同型样本**：>12pt 大分歧的 LLM 直赌历史战绩是 0/3 亏 -$4.87（该文档当时
点名本仓 "$2.45 敞口" 需盯防）。今天止损机制生效，把潜在最大亏损（5×49c=$2.45）截断到 -$1.40，**纪律执行
到位**，但根因（大分歧 LLM 直赌结构性负 EV）与 07-11 判决完全吻合，等于又一次实证支持既有停摆决议，
非新信息。

**KXFEDDECISION-26JUL-H0** no x3@21c → **止盈平仓@33c**（非终局结算），pnl **+$0.28**。模型 q=0.73 vs 市场
q=0.795（分歧仅 3.5pt，低于 3.5pt 的活跃门槛，rationale 明确写"不追随 CME 噪声 → 本应 no-trade"，该仓是
run1 遗留未平的老仓）。**判定：过程纪律良好但非模型验证** —— 止盈机械执行及时锁盈，赢面主要来自价格朝止盈
方向移动，而非入场时就存在的可验证边际（入场本身在事后复盘看已低于活跃门槛）。

## 校准趋势

- **shortcycle**：今日无新样本（通道停用），停留在 n=35，Brier 0.0259 vs 市场 0.0347，自 07-08 起持续跑赢
  市场，差距稳定在 0.008-0.009。
- **weather** 逐日 Brier(model)/Brier(market)：
  07-07 0.2897/0.2307 → 07-08 0.3256/0.2277 → 07-09 0.3233/0.2742 → 07-10 0.3247/0.2928 → 07-11 0.3247/0.3100
  → 07-12 0.3226/0.3328（首次反超）→ **07-13 0.3103/0.3268**（模型 Brier 本身下降，且继续跑赢市场，差距
  从 -0.0102 扩大到 +0.0165）。n 从 27 涨到 30，仍低于本项目晋升门槛惯用的 n≥50，**趋势正向但样本仍不足以
  下结论**。
- **favorite 桶偏差**（`mktcal`，全市场 8291 样本 τ≤20min）：全部十个概率桶偏差绝对值 ≤4.3pt（最大在
  0.4-0.5 桶 +4.3pt，0.2-0.3 桶 +2.9pt），favorite 侧细分桶（0.75-1.00）偏差 ≤2.8pt —— 市场本身校准良好，
  无强烈系统性 favorite-longshot 偏差可收割。
- **H9（午夜重开滞后富集）**：shortcycle 停用中，无新的小时级 lag 门通过率数据可用于检验该假说；管线中也
  尚无专门的"逐小时 lag-gate pass 分布"统计口径（仅有 shortcycle 内部 `lag_only` 门的二元通过/拒绝，未
  按小时聚合导出）。维持归档/待日历到期状态，**本身是一项待办的测量基础设施缺口，非结论性证据**。

## 回归测试

- `tests/test_ledger_live.py`：**PASS**（ALL LEDGER LIVE-STATE TESTS PASSED）
- `tests/test_swing.py`：**PASS**（take-profit pnl=$+1.95, swing_pnl=$+1.95）
- 今日提交历史（`git log --since '2026-07-13 00:00' -- src/`）**无任何 `src/*.py` 改动** —— 全部提交都是
  quant_loop 自动的 data/reports 更新，**不涉及下单路径，无需手动重跑 `tests/test_failures.py`**。

## 明日提案

1. **quant_loop 14.4 小时静默空档（07-13 07:32→21:55），本项目日志历史中首次出现**：当前 watchdog 机制
   （`scripts/watchdog.ps1` 180s 自查）疑似只做"进程是否存在"的普查，而非"日志/提交是否新鲜"的活性检测——
   若循环挂死但进程句柄仍在，或 watchdog 自身与循环一起失效，现有机制不会告警。提议：加一层基于
   `data/quant_loop.log` 最后一行时间戳的陈旧度检查（例如 >20min 无新行则视为不健康并触发重启+记日志），
   而不仅是进程存在性 census。不改任何交易参数，纯运维基础设施，FREEZE-14 不适用。
2. **h15 今日 21:55 触发 HARD STOP（累计已实现 -$4.56，暂停待复核）**：当前 -> 提议不擅自调整任何 h15
   阈值/尺寸（FREEZE-14 冻结中），仅记录该次熔断供 07-23 边界日与既有 07-11 观察项（h15maker-isolation-
   drawdown-observation）一并裁决是否 KILL/继续隔离观察。
3. **MEDNOM 止损平仓再次印证 07-11 CORE 五人会判决**（>12pt 大分歧 LLM 直赌 0/3 亏 -$4.87 的同一机制，
   今日追加一例 -$1.40）：无需新提案，本身是既有判决的实证支持，供 07-23 复活提案队列参考，强化"分歧
   >15-20pt 跳过"门槛的数据基础。
4. **weather Brier 连续两日反超市场且差距扩大**（07-12 差 +0.0102 → 07-13 差 +0.0165，n 27→30）：当前
  -> 提议继续观察至 n≥50（对齐 blindai 晋升门槛）再评估是否值得升级预算，不改任何阈值。
5. **H9 假说缺少专用的逐小时 lag-gate 通过率统计**：提议若未来重启 shortcycle 相关工作，顺手加一个按小时
   聚合的 `lag_only` 门通过/拒绝计数导出，供 H9 归档/复议判据表使用；纯测量基础设施，非交易参数改动。
